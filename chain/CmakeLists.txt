cmake_minimum_required(VERSION 3.20)

project(MATRIX)
add_library(chain_matrix_lib INTERFACE)
target_include_directories(chain_matrix_lib INTERFACE ${CMAKE_CURRENT_SOURCE_DIR})

add_executable(chain)
target_sources(chain PRIVATE main.cpp)
target_link_libraries(chain PRIVATE matrix_lib chain_matrix_lib)
add_subdirectory(tests/benchmark)






